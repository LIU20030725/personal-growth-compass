# 测试命令与最终状态

## 最终自动化证据

- `npm test -- --run`：40 个测试文件、181 项测试全部通过。
- 健康相关：10 个测试文件、35 项测试（领域、存储、媒体、hook、组件、App 集成、axe、计时、20 条训练夹具、容量）。
- `npm run build`：TypeScript 与 Vite 生产构建通过；1,630 modules transformed。
- `git diff --check`：通过，仅有 Windows CRLF 提示，无空白错误。
- 扫描：`src/health` 无 console、fetch、XHR、sendBeacon、外部 URL；无秘密字面量。

## 共享 App.tsx 回归

- `src/App.test.tsx` 10 项通过：财富页、账户、流水、转账、导航。
- 任务集成、情绪集成、健康集成测试通过。
- 共享 App 仅增加健康 view 的 `health-shell` / `health-sidebar` class，用于窄屏健康布局；其他 view 条件不变。

## 修复摘要

1. 修复同一 render 内连续 mutation 覆盖前一次记录；身体相同快速提交 800ms 幂等。
2. 增加严格备份 manifest、checksum、大小/数量/ID/引用/媒体预检与原子替换回滚。
3. 增加照片 MIME/大小限制、读取缩略图、永久删除与孤儿清理。
4. 补齐水/睡眠/活动/精力/训练边界校验、本地日期键和 7/30/90 窗口逻辑。
5. 草稿持久化；休息计时改用绝对目标时间并支持暂停、继续、延长、跳过、刷新恢复。
6. 导入 UI 改为预检摘要→确认替换；垃圾箱永久删除需确认。
7. 修复移动遮挡和触控目标，关闭的日常指标真正隐藏，自定义饮水可记录。
8. 补齐训练项目归档入口，以及身体、餐食、日常、训练四类垃圾箱的永久删除确认。

## 当前状态

- 未合入 main，未 push。
- 本文记录的是自动化与单人浏览器验收事实，不自行宣布产品通过。
- 仍等待总控判断的人工门禁和已知限制见 `07-覆盖矩阵.md`。
