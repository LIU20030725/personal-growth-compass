# 项目总上下文

最后更新：2026-07-05

## 当前项目

这是一个「个人成长进步规划 App」，当前正在优先优化其中的「经济系统」模块。

技术栈：

- React + TypeScript
- Vite
- Vitest + Testing Library
- 样式文件：`src/styles.css`
- 主页面组件：`src/App.tsx`
- 财务计算逻辑：`src/finance/financeEngine.ts`

运行方式：

```bash
npm run dev
```

本地预览地址：

```text
http://127.0.0.1:5173/
```

## 当前最重要的工作方向

现在不是在做整个 App 的全部人生系统，而是在单独优化「经济系统」。

经济系统的目标是：

- 先以账户为核心管理钱。
- 用户可以新增资金账户、理财账户、应收账款。
- 用户可以对某个账户记录收入、支出、盈收、亏损、转账。
- 用户可以看到净资产、本月收入、本月支出、本月结余、账户余额、月度流水、存钱日历。
- 页面要保持清楚、可扫、像一个财务任务面板。

## 当前 UI 布局

当前 UI 已经恢复为纵向仪表盘布局，不要再改成强行一屏三栏网站布局。

当前页面结构大致是：

1. 固定顶部栏：`GOLDQUEST FINANCE`
2. 左侧角色栏：角色等级、导航、英雄属性
3. 主内容区：
   - 净资产 Hero
   - 三个关键指标：本月收入、本月支出、本月结余
   - 三组账户卡：资金账户、理财账户、应收账款
   - 本月收入支出入口
   - 存钱日历

重要规则：

- 如果用户没有明确要求改变页面布局、整体结构、模块位置或信息架构，不允许主动改 UI 布局。
- 可以优化组件内部视觉、交互、字段、弹窗、表单、状态和局部样式。
- 不要把纵向布局改成横向布局。
- 不要为了“一屏显示”而压缩、重排或移动已有主模块，除非用户明确要求。

这个规则也已经写入 `STYLE.md` 的「布局保护优先」。

## 当前已实现的经济系统功能

### 1. 净资产总览

页面顶部展示：

- 净资产
- 经济评分
- 记一笔按钮

净资产由账户、应收、不动产估值和负债等数据计算。

### 2. 三类账户

当前账户分为：

- 资金账户：银行卡、微信钱包、公积金账户等
- 理财账户：股票、基金、港股券商、美股券商等
- 应收账款：报销、借出款、项目尾款等

每组账户可以展开，下方展示具体账户。

点击具体账户会打开账户详情页。

### 3. 快速记账

点击 `记一笔` 会打开「记录一笔现金流」表单。

当前表单已支持：

- 选择收入或支出
- 选择资金账户
- 填写金额、分类、日期、备注
- 实时显示所选资金账户余额
- 输入金额后显示扣除后余额或入账后余额

保存后会同时发生两件事：

- 写入全局现金流记录，用于本月收入、本月支出、日历和分析统计
- 写入对应资金账户流水，并自动更新该账户余额

例如选择微信钱包支出 100，微信钱包余额会自动减少 100。

### 4. 账户详情页

点击某个账户后进入账户详情页。

资金账户使用：

- 收入
- 支出

理财账户使用：

- 盈收
- 亏损

应收账款使用：

- 收回
- 减少

账户详情里还支持账户转账。

转账只影响账户余额和账户流水，不计入本月收入/支出。

### 5. 本月收入支出详情页

主页有一个 `本月收入支出` 入口。

点击后打开独立弹出页，不在主页下拉展开，避免主页过长。

弹出页包括：

- 左侧：本月支出去向饼图
- 左侧下方：总收入、总支出、本月结余、储蓄率
- 右侧：每日收支记录
- 顶部：日期下拉，可以选择某一天查看当天记录

这部分参考了 iCost / 银行流水类 App 的记录逻辑。

### 6. 存钱日历

存钱日历按月份展示 2026 年每月结余。

点击某个月份，会弹出该月详情，展示：

- 总收入
- 总支出
- 当月收入/支出来源

## 当前设计风格

设计风格来源：

- `STYLE.md`
- `exports/stitch-visual-refined-ui-spec.md`
- Stitch 视觉参考：光明冒险财务大厅 - 视觉精修版

核心风格：

- 明亮 RPG
- 硬边报表
- 白底/浅灰底
- 黑色 2px 边框
- 实体硬阴影
- 金色主按钮
- 英文微标签
- 左侧角色成长面板

不要回到早期深色玻璃拟态风格。

## 当前 Git / 版本情况

项目版本：

```text
package.json version: 2.0.0
releaseDate: 2026-07-02
```

之前已发布：

- GitHub Release：`V2.0`
- 远端分支：`codex/economy-system-v0.1-0628`
- 本地长期工作分支曾整理为：`codex/economic-system/main`

当前本地还有未提交修改，主要来自最近的经济系统 UI 和交互迭代。

## 当前验证状态

最近一次验证结果：

```bash
npm test
```

结果：

```text
10 tests passed
```

最近一次构建：

```bash
npm run build
```

结果：通过。

## 重要注意事项

- `canvas/` 和 `docs/docs_bak/superpowers/` 在本机存在权限问题，工具扫描时可能报 `EPERM`。
- `vite.config.ts` 已经排除了 `canvas/`、`docs/docs_bak/`、`exports/`，避免测试和开发服务扫描这些目录。
- `STYLE.md` 当前是重要 UI 风格源文档。
- `PROJECT_CONTEXT.md` 是后续接上下文时优先阅读的项目总文档。

## 下一步建议

优先继续完善经济系统，不要马上扩展到能力、身体、情绪系统。

下一步可以做：

- 优化账户详情页的信息层级
- 优化本月收入支出弹出页的记录筛选
- 增加记录编辑/删除
- 增加数据持久化，避免刷新后数据丢失
- 增加更多账户类型字段
- 最后再考虑提交并推送当前版本
